[package]
name = "gravity_game"
version = "0.1.0"
edition = "2021"

[features]
default = []
debug_info = ["galaxy_engine/debug_info"]

[dependencies]
galaxy_engine = { path = "../galaxy_engine" }
winit = "0.30.5"
thiserror = "1.0.64"
env_logger = "0.11.5"
