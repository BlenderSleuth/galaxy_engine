[package]
name = "galaxy_engine_config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[features]
ron = ["serde", "dep:ron"]

[dependencies]
serde = { workspace = true, optional = true }
ron = { workspace = true, optional = true }
